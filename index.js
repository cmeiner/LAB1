const express = require('express')
const fs = require('fs')
const port = 3000
const server = express()

const boxFile = './data/boxes.json'
const boxes = require(boxFile)

server.get

server.post

server.put

server.delete